
extends ../layout

block content
  .container
    h1.text-center Cursos Matriculados
    hr

    .table-responsive.tamTabla
      table.table.table-striped.table-hover
        thead.bg-primary
          tr
            th(scope='col') #
            th(scope='col') Título del Curso
            th(scope='col') E.A.P
            th(scope='col') Ciclo
            th(scope='col') Profesor
            th(scope='col') 
        tbody
          each curso, index in cursos
            tr
              th(scope='row') #{index + 1}
              td  #{curso.idCourse.title}
              if curso.idCourse.eap == 1
                td  Ingeniería de Sistemas
              else
                td  Ingeniería de Software
              td  #{curso.idCourse.cycle}
              td  #{curso.idCourse.idTeacher.names} #{curso.idCourse.idTeacher.surnames}
              td 
                a.btn.btn-primary(href=`/alumno/cursos/${curso.idCourse._id}`, role='button') Ver más